export default function Terms(){return <Policy title="Terms of use" text="Backhaul is a route-matching MVP. Captains must hold valid permits and documents. Users must provide accurate route, capacity and goods information. Mock payments and OTPs are for local demonstration only."/>} function Policy({title,text}:{title:string;text:string}){return <div className="page-shell section-pad"><h1 className="display-title">{title}</h1><p className="body-copy mt-6 max-w-3xl">{text}</p></div>}

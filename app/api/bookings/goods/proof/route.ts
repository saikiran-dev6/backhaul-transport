import { NextRequest,NextResponse } from "next/server";import { requestUser } from "@/lib/auth";import { db } from "@/lib/db";import { z } from "zod";
const schema=z.object({bookingId:z.string(),deliveryProofUrl:z.string().startsWith("/uploads/")});export async function PATCH(request:NextRequest){const auth=await requestUser(request);if(!auth)return NextResponse.json({error:"Login required"},{status:401});const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Invalid proof"},{status:400});const booking=await db.goodsBooking.findUnique({where:{id:parsed.data.bookingId},include:{goodsRequest:true,trip:{include:{driver:true}}}});if(!booking||![booking.goodsRequest.senderId,booking.trip.driver.userId].includes(auth.userId))return NextResponse.json({error:"Booking not found"},{status:404});const updated=await db.goodsBooking.update({where:{id:booking.id},data:{deliveryProofUrl:parsed.data.deliveryProofUrl,deliveryStatus:"DELIVERED"}});await db.goodsRequest.update({where:{id:booking.goodsRequestId},data:{status:"DELIVERED"}});return NextResponse.json({booking:updated})}

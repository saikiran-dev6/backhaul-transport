"use client";
import Link from "next/link";import { AuthShell } from "@/components/AuthShell";import { useLanguage } from "@/components/Providers";import type { Language } from "@/lib/i18n/translations";
export default function LanguagePage(){const {language,setLanguage}=useLanguage();return <AuthShell title="Choose your language" copy="The navbar, landing page and key calls to action update immediately."><div className="grid gap-3">{[["en","English","Return Trips Made Useful"],["te","తెలుగు","తిరుగు ప్రయాణాలు ఉపయోగకరం"],["hi","हिन्दी","वापसी यात्राएँ उपयोगी बनाएं"]].map(([id,name,copy])=><button onClick={()=>setLanguage(id as Language)} key={id} className={`rounded-2xl border p-4 text-left ${language===id?"border-brand-500 bg-brand-50":"border-slate-200"}`}><strong>{name}</strong><span className="ml-3 text-sm text-slate-500">{copy}</span></button>)}<Link className="btn-primary mt-2" href="/">Continue</Link></div></AuthShell>}

"use client";
import { FormEvent,useState } from "react";import Link from "next/link";import { AuthShell } from "@/components/AuthShell";
export default function Forgot(){const [sent,setSent]=useState(false);return <AuthShell title="Reset your password" copy="The MVP displays a safe mock confirmation; connect EMAIL_HOST for production delivery.">{sent?<div className="rounded-2xl bg-eco-50 p-5 text-center"><strong>Reset link simulated</strong><p className="mt-2 text-sm text-slate-600">No email was sent in local mode.</p><Link className="btn-primary mt-4" href="/login">Back to login</Link></div>:<form onSubmit={(e:FormEvent)=>{e.preventDefault();setSent(true)}} className="grid gap-4"><label><span className="label">Registered email</span><input required type="email" className="field"/></label><button className="btn-primary">Send reset link</button></form>}</AuthShell>}
